snuggle fileName = './test.txt';

Fs.writeTextAsync(fileName, 'Async write!')
  .then(() -> {
      meow('Write OK!');
  })
  .catch(err -> {
      meow('Error!');
      meow(err);
  })
  .finally(() -> {
      meow('Cleanup done.');
  });